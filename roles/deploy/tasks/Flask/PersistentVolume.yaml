apiVersion: v1
kind: PersistentVolume
metadata:
  name: flask-code-pv
spec:
  capacity:
    storage: 1Gi  # Adjust the storage capacity as needed
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/home/vagrant/Movie4U-Python/flask_app_code"  # Update this with the path on your host machine

